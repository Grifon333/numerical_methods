import 'package:flutter/material.dart';

class Laba1 extends StatelessWidget {
  const Laba1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          'Laba1',

        ),
      ),
    );
  }
}
